window.JourneyTimeline = function({ timeline = [], activeScenario, safeBeacon }) {
  const exportSummary = () => { const payload = { generatedAt: new Date().toISOString(), traveler: activeScenario.travelerName, lastSafeBeacon: safeBeacon, events: timeline }; const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'saferoute-incident-summary.json'; link.click(); URL.revokeObjectURL(url); };
  const tone = { safe:'#10B981', warning:'#F59E0B', emergency:'#EF4444', system:'#94A3B8' };
  return <section className="srg-timeline-panel"><div className="srg-ai-header"><div><h3>Journey Evidence Timeline</h3><p>Journey data is visible only to authorized safety contacts and administrators.</p></div><button className="srg-btn srg-btn-primary srg-btn-sm" onClick={exportSummary}>Export incident summary</button></div>{safeBeacon && <div className="srg-timeline-beacon"><b>Latest Safe Beacon</b><span>{safeBeacon.travelerName} · {safeBeacon.networkStatus} network · battery {safeBeacon.battery}% · {new Date(safeBeacon.timestamp).toLocaleTimeString()}</span></div>}<div className="srg-timeline-list">{timeline.length === 0 ? <div className="srg-empty-request">No recorded events yet. Use the demo controls or Safe Beacon network simulation to create the timeline.</div> : timeline.map(event => <div className="srg-timeline-event" key={event.id}><i style={{ background:tone[event.tone] || tone.system }}></i><div><b>{event.type.replaceAll('_',' ')}</b><span>{event.message}</span></div><time>{new Date(event.timestamp).toLocaleTimeString()}</time></div>)}</div></section>;
};
